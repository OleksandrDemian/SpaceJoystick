<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.oleksandr.spacejoystick.HomeActivity$PlaceholderFragment"
    android:background="@color/background">

    <Button
        android:text="Join the game"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/btnConnect"
        android:background="@color/internalElements"
        android:textColor="@color/colorAccent"
        android:elevation="9dp"
        android:layout_centerVertical="true"
        android:layout_alignParentEnd="true"
        android:layout_toEndOf="@+id/section_label"
        android:layout_alignParentStart="true"
        android:layout_margin="5sp" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:ems="10"
        android:id="@+id/ip"
        android:textAlignment="center"
        android:textColor="@color/colorAccent"
        android:hint="Type game ip here"
        android:inputType="text"
        android:fontFamily="monospace"
        android:layout_above="@+id/btnConnect"
        android:layout_alignParentStart="true"
        android:layout_alignParentEnd="true" />

</RelativeLayout>
